# 镜像构建上下文里不需要的东西。
#
# deploy/Dockerfile 会 `COPY . .` 后再编译，所以这份名单直接决定镜像里多出什么。
# 尤其是 .git 与运行期数据：前者是几十 MB 的噪音，后者是**节点身份与密钥**
# （data/ 下有 node-id 与 jwt-secret），进镜像层就擦不掉了。
.git
.gitignore
.github

# 构建产物
dist/

# 运行数据：库 + blobs + 节点身份 + 密钥
data/
*.db
*.db-wal
*.db-shm

# 文档与本地环境
README.md
CHANGELOG.md
LICENSE
deploy/
scripts/
.env
*.log
.DS_Store
